{
	"name": "M3U8 Content fix",
	"version": "0.1",
	"manifest_version": 2,
	"description": "webRequest listener that modifies the content type header of m3u8 files from text/plain to application/x-mpegURL",

	"permissions": [
		"webRequest",
		"webRequestBlocking",
		"*://*/*/*.m3u", 
		"*://*/*/*.m3u8"
	],

	"background": {
		"scripts": ["background.js"]
	}

}
